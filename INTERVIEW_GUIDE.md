# RAG 项目面试追问速查

## 1. 你这个项目的完整流程是什么？

PDF 文献先按页解析，清洗文本后使用递归字符切分器生成带重叠的 chunk；每个 chunk 保留文件名、页码、chunk_id 等元数据。随后使用 BGE 模型生成向量并写入 FAISS。用户提问时先对 query 做 embedding，在 FAISS 中召回 Top-K 文献片段，再把问题和片段拼成 Prompt 交给 LLM。系统要求模型只根据检索结果回答，并在答案中用 `[1][2]` 标注证据来源，最后把编号映射回论文文件和页码。

## 2. 为什么要做 Chunk？为什么默认 800 / 120？

整篇论文直接 embedding 会导致语义过于宽泛，也容易超过模型上下文窗口；切得太小又容易破坏完整定义、公式说明和实验上下文。因此用中等长度 chunk，并保留 overlap 来减少跨段信息被截断的概率。800/120 是初始工程参数，不应该说是“最优值”，实际应通过 Hit@K、人工检查或更完整的 RAG 评测确定。

## 3. 为什么用 RecursiveCharacterTextSplitter？

它会优先按段落、换行、句号等自然边界切分，只有超过目标长度时才逐级使用更细粒度分隔符。相比固定字符硬切，它更容易保留论文语义完整性。

## 4. 为什么选 BGE Embedding？

项目面向中文和中英混合的水声专业文献，需要一个可本地部署的语义向量模型。BGE 系列适合做语义检索，且可以与 FAISS 组合构建离线向量索引。Embedding 模型在这个项目中的职责不是生成答案，而是把 query 和文档映射到同一向量空间用于相似度检索。

## 5. 为什么用 FAISS？

项目属于个人/单机知识库，数据规模主要是几十到几百篇论文，不需要先引入分布式向量数据库。FAISS 本地部署简单、检索速度快、无额外服务依赖，适合做原型和算法验证。如果后续做多人在线服务，再考虑 Milvus、Qdrant、Elasticsearch 等方案。

## 6. Top-K 越大越好吗？

不是。K 太小可能漏掉关键证据，K 太大会引入无关上下文，增加 token 消耗并干扰模型判断。因此项目把 Top-K 做成可配置参数，并提供 retrieval-only 模式和 Hit@K 评测，方便对比不同 K 值。

## 7. 引用是怎么实现的？

不是让模型自己编文献。检索到的文档片段在送入 Prompt 前会被程序编号为 `[1] [2] ...`，每个编号对应已保存的 source、page、chunk_id。Prompt 要求模型只能引用这些编号，最终程序再把编号对应的文件名和页码展示出来。

## 8. 你怎么降低幻觉？

主要做了三层限制：第一层是检索，只把与问题相关的论文片段送给模型；第二层是 Prompt，明确要求证据不足时拒绝编造；第三层是引用，把技术结论与可回溯的检索片段绑定。需要强调的是：RAG 只能降低无依据生成风险，不能保证完全消除幻觉。

## 9. 你怎么评价项目效果？

项目当前提供 Retrieval Hit@K 的轻量评测，用测试问题和期望关键词检查 Top-K 检索结果是否包含关键证据。更完整的方案可以继续增加 Recall@K、MRR，以及针对最终回答的 Faithfulness、Context Recall、人工事实一致性评分。简历中如果写具体提升百分比，应以自己实际跑出的实验数据为准。

## 10. 如果检索结果不准，你会怎么排查？

先用 `python main.py search` 只看检索结果，排除 LLM 影响。然后依次检查 PDF 文本解析质量、chunk 大小/重叠、Embedding 模型、query 表述、Top-K；仍然不理想时加入 BM25 混合检索或 Reranker。

## 11. Rerank 为什么有用？

第一阶段向量检索追求高召回，可以先取 Top-20；Reranker 再对 query-document pair 做更精细的相关性判断，重新排序后只把最相关的 Top-4/Top-6 送给 LLM。这样通常能减少“语义看起来相近但实际不回答问题”的片段。

## 12. RAG 和微调有什么区别？

RAG 主要改变推理时上下文，知识库可以直接增删文档，不必重新训练模型；微调主要改变模型参数，更适合学习稳定的任务行为、格式、风格或领域模式。对于经常增加论文的知识库，RAG 更新成本更低。

## 13. 这个项目和 PINN 有什么联系？

联系是工程思想层面的：都在利用外部先验限制模型自由度。区别是 PINN 通常把物理方程约束放进训练损失或训练过程，而 RAG 是把检索到的事实知识加入推理上下文。不要在面试中说二者算法机制相同。

## 14. 目前最大的不足是什么？

当前 PDF 主要依赖文本抽取，对扫描版 PDF、复杂公式和图表的处理能力有限；检索还是纯 Dense Retrieval，没有加入 BM25、Reranker 和 Parent Document Retrieval；评测目前是轻量检索评测，后续需要建立更系统的领域 QA 测试集。
